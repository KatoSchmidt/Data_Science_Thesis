# @package _global_
defaults:
  - /model: unet_convnext

trainer:
  loss_fn:
    _target_: the_well.benchmark.metrics.MSE
  formatter: channels_first_default

optimizer:
  _target_: torch.optim.AdamW
  lr: 5E-3


name: unet_convnext
