# @package _global_
defaults:
 - /model: sinenet

trainer:
  loss_fn:
    _target_: the_well.benchmark.metrics.MSE
  formatter: sinenet_formatter

optimizer:
  _target_: torch.optim.AdamW
  lr: 2E-4

name: SineNet_4_(64-140)

# trainer:
#   loss_fn:
#     _target_: the_well.benchmark.metrics.ScaledLpLoss
#     p: 2
#     reduction: none
#   formatter: sinenet_formatter