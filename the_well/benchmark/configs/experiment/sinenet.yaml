# @package _global_
defaults:
 - /model: sinenet

trainer:
  loss_fn:
    _target_: the_well.benchmark.metrics.MSE
  formatter: sinenet_formatter

optimizer:
  _target_: torch.optim.AdamW
  lr: 2E-4

name: 4-run1

